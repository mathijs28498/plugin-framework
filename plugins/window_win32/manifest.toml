interface_name = "window"
interface_version = "1"
plugin_name = "win32"
module_path = "${build_dir}/window_win32.dll"
supported_lifetimes = ["singleton"]
shutdown = true

[dependencies] 
logger = {}
environment = {}
