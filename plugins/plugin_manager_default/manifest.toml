interface_name = "plugin_manager"
interface_version = "1"
plugin_name = "default"
module_path = "${build_dir}/plugin_manager_default.dll"
supported_lifetimes = ["singleton"]
init = true
shutdown = true

[dependencies]
logger = { deferred = true }
environment = { deferred = true }

