interface_name = "logger"
plugin_name = "console"
init = true
shutdown = true
supported_lifetimes = ["singleton"]
module_path = "C:/Users/Blue-WaveMathijsFran/Documents/GitHub/notepad-c/build/bin/Debug/logger_console.dll"

# TODO: Check if this is still necessary, maybe this can just be in the second shutdown phase 
# exported_declarations = [
#     "struct LoggerInterfaceContext;",
#     "void logger_console_on_program_exit(struct LoggerInterfaceContext *context, int exit_code);",
# ]
