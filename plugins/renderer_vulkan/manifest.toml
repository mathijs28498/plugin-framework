interface_name = "renderer"
interface_version = "1"
plugin_name = "vulkan"
module_path = "${build_dir}/renderer_vulkan.dll"
supported_lifetimes = ["singleton"]
init = true
shutdown = true

[dependencies]  
logger = {}
window = {}
arena_allocator = {}